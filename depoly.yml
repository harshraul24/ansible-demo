---
- name: Deploy My App
  hosts: your_target_hosts
  tasks:
    - name: Copy application files
      copy:
        src: /path/to/your/app
        dest: /path/on/target/server

    - name: Restart the application
      shell: systemctl restart your-app-service
